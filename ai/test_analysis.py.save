from analyze_quote import analyze_quimport json
from analyze_quote import analyze_quote
from reference_data import load_reference_prices

ref = load_reference_prices()

sample_quote = [
    {"material": "cement", "quantity": 100, "unit": "bag", "unit_price": 6.00},
    {"material": "steel bar", "quantity": 200, "unit": "kg", "unit_price": 1.00},
    {"material": "paint (interior)", "quantity": 10, "unit": "gallon", "unit_price": 30.00},
]

result = analyze_quote(sample, ref)
print(json.dumps(result, indent=2))from reference_data import load_reference_prices

ref = load_reference_prices()

sample_quote = [
    {"material": "cement", "quantity": 100, "unit": "bag", "unit_price": 6.00},
    {"material": "steel bar", "quantity": 200, "unit": "kg", "unit_price": 1.00},
    {"material": "paint (interior)", "quantity": 10, "unit": "gallon", "unit_price": 30.00},
]

result = analyze_quote(sample_quote, ref)
print(json.dumps(result, indent=2))
